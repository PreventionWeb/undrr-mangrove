import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { CtaButton } from './CtaButton';
import * as CtaButtonStories from './CtaButton.stories';

export const getCaptionForLocale = locale => {
  switch (locale) {
    case 'english':
      const engText = { detail: 'Read more' };
      return engText;
    case 'arabic':
      const arabicText = { detail: 'اقرأ أكثر' };
      return arabicText;
    case 'japanese':
      const japaneseText = { detail: '続きを読む' };
      return japaneseText;
    default:
      return { detail: 'Read more' };
  }
};

<Meta of={CtaButtonStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Buttons

Mangrove's standard button component. Buttons are designed as call-to-action (CTA) elements — they should always direct the user toward a clear next step such as "Read more", "Sign up", or "View documentation."

#### When to use:

- Use buttons to drive a specific user action, not for navigation between pages (use links instead)
- Each page should have one primary action. Secondary and tertiary options should be used sparingly
- If there are multiple choices, one button should be the default (primary) and the others secondary or tertiary

#### Variants

| Class                                              | Purpose                                                |
| -------------------------------------------------- | ------------------------------------------------------ |
| `.mg-button.mg-button-primary`                     | Main call to action — filled background, high contrast |
| `.mg-button.mg-button-secondary`                   | Supporting action — lower visual weight than primary   |
| `.mg-button.mg-button-primary.mg-button-outline`   | Primary emphasis with a transparent surface            |
| `.mg-button.mg-button-secondary.mg-button-outline` | Secondary emphasis with a transparent surface          |
| `.mg-button.mg-button-primary.mg-button-cta`       | Editorial navigation with a circular direction badge   |
| `.mg-button.disabled`                              | Inactive state — not clickable                         |

### Content

- Use clear, action-oriented labels: "Download report", "Sign up", not "Click here"
- Keep labels short — ideally 2-3 words

## Behaviors

Button colors adapt to the active theme (UNDRR, PreventionWeb, DELTA, etc.). In dark contexts like heroes and CTAs, buttons invert to ensure contrast.

<Canvas of={CtaButtonStories.DefaultButtons} meta={CtaButtonStories} />

### Editorial CTA

Use `Variant="CTA"` for navigational prompts in cards, heroes, and section
headers. Keep consent, form submission, and utility actions on the default
filled-button treatment.

<Canvas of={CtaButtonStories.EditorialCta} meta={CtaButtonStories} />

### All variants

Primary, secondary, and disabled buttons on light and dark backgrounds for easy comparison. Switch themes in the toolbar to see how each variant adapts.

<Canvas of={CtaButtonStories.AllVariants} meta={CtaButtonStories} />

### Usage

- Apply `.mg-button` as the base class, then add an emphasis class (`.mg-button-primary`, `.mg-button-secondary`). Add `.mg-button-outline` when the action needs an unfilled treatment.
- Copy the HTML from the HTML tab of the canvas

### Interactions

- Hover state applies to the entire button

## Changelog

- **1.6** — 2026-09-01 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)): Made the editorial treatment an explicit `Variant="CTA"` option using the descriptive `.mg-button-cta` modifier. CtaButton now defaults to the conventional shared button treatment; cards and heroes opt into the editorial variant intentionally.
- **1.5** — 2026-08-31 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)): Added a reduced-motion-aware `scale(0.96)` pressed state to conventional buttons. Editorial link CTAs keep their labels stable and retain the directional badge-only response.
- **1.4** — 2026-08-31 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)): Refreshed React `CtaButton` instances as compact editorial links with an optically aligned circular direction badge, tactile press state, RTL direction, and context-aware hero colours. Raw `.mg-button` controls retain their conventional treatment.
- **1.3.1** — 2026-08-31: Buttons now transition background/border/text colours smoothly on hover (gated by `prefers-reduced-motion`), and the keyboard focus indicator is a clearer token-driven ring (neutral separator + `--mg-color-focus-ring`) instead of a low-contrast same-colour glow.
- **1.3** — 2026-08-26: Add composable `.mg-button-outline` modifier for primary and secondary emphasis variants.
- **1.2** — 2026-04-13: `.mg-button` now declares an explicit `font-family: $mg-font-family-headings;` (was inheriting body Roboto). Aligns with the existing `:lang(ar)` override which already used `$mg-font-family-arabic-headings` — Latin and Arabic now match.
- **1.1** — 2026-03-22: Add "All variants" comparison story (light + dark backgrounds). Fix secondary button contrast to meet WCAG AA (blue-800/blue-700, was blue-600/blue-400). Fix disabled button contrast (neutral-500 bg + white text). Remove dead `mg-button-arrow` class and `For_Primary` prop. Add `:focus-visible` ring for buttons on dark backgrounds. Tokenize secondary button and hero secondary button colors for theme overriding.
- **1.0** — 2022-12-12: Released component
