import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { CtaButton } from './CtaButton';
import * as CtaButtonStories from './CtaButton.stories';

export const getCaptionForLocale = locale => {
  switch (locale) {
    case 'english':
      const engText = { detail: 'Read more' };
      return engText;
    case 'arabic':
      const arabicText = { detail: 'اقرأ أكثر' };
      return arabicText;
    case 'japanese':
      const japaneseText = { detail: '続きを読む' };
      return japaneseText;
    default:
      return { detail: 'Read more' };
  }
};

<Meta of={CtaButtonStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Buttons

Mangrove's standard button component for call-to-action elements like "Read more", "Sign up", or "View documentation."

## When to use

- Drive specific user actions (use links for page navigation)
- One primary action per page; secondary/tertiary should be sparse
- For multiple choices, mark one as primary and others secondary/tertiary

#### Variants

| Class                                              | Purpose                                                |
| -------------------------------------------------- | ------------------------------------------------------ |
| `.mg-button.mg-button-primary`                     | Main call to action — filled background, high contrast |
| `.mg-button.mg-button-secondary`                   | Supporting action — lower visual weight than primary   |
| `.mg-button.mg-button-primary.mg-button-outline`   | Primary emphasis with a transparent surface            |
| `.mg-button.mg-button-secondary.mg-button-outline` | Secondary emphasis with a transparent surface          |
| `.mg-button.mg-button-primary.mg-button-cta`       | Editorial navigation with a circular direction badge   |
| `.mg-button.disabled`                              | Inactive state — not clickable                         |

## Content

- Use clear, action-oriented labels: "Download report", "Sign up" (not "Click here")
- Keep labels to 2–3 words

## Behaviors

Button colors adapt to the active theme. In dark contexts, buttons invert for contrast.

<Canvas of={CtaButtonStories.DefaultButtons} meta={CtaButtonStories} />

## Editorial CTA

Use `Variant="CTA"` for navigational prompts in cards, heroes, and section headers. Use the default filled-button treatment for consent, form submission, and utility actions.

<Canvas of={CtaButtonStories.EditorialCta} meta={CtaButtonStories} />

## All variants

Primary, secondary, and disabled buttons on light and dark backgrounds. Switch themes in the toolbar to see how each variant adapts.

<Canvas of={CtaButtonStories.AllVariants} meta={CtaButtonStories} />

## Usage

- Apply `.mg-button` as the base class, then add an emphasis class (`.mg-button-primary` or `.mg-button-secondary`)
- Add `.mg-button-outline` for unfilled treatment
- Wrap adjacent actions in `.mg-buttons` for consistent, RTL-safe spacing (don't add margins to individual buttons)
- Copy HTML from the HTML tab in the canvas

## Interactions

- Hover state applies to the entire button

## Changelog

- **1.4** — 2026-09-10 ([#1113](https://github.com/unisdr/undrr-mangrove/pull/1113)): The arrow bubble slides a few pixels toward its destination on hover, flipped in RTL and suppressed under `prefers-reduced-motion`, where the shift would be a jump rather than a gesture.

- **1.3.2** — 2026-09-08 ([#1092](https://github.com/unisdr/undrr-mangrove/issues/1092)): Arabic typography no longer applies to a button whose own language is not Arabic.
- **1.3.1** — 2026-09-01 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)):
  - Refreshed React `CtaButton` instances as compact editorial links with an optically aligned circular direction badge, tactile press state, RTL direction, context-aware hero colours, and safe wrapping for narrow German, French and Arabic labels.
  - Made the editorial treatment an explicit `Variant="CTA"` option; conventional form and utility actions retain their filled button treatment.
  - Switched button labels from Roboto Condensed to regular Roboto, strengthened and optically centred the CTA chevron, and kept label/badge geometry stable across hover and focus.
  - Softened conventional buttons with the shared small-radius token, replaced physical adjacent margins with the RTL-safe `.mg-buttons` gap container, and hardened disabled links so caller props cannot restore navigation.
  - Added focused behaviour and accessibility coverage, including smoother hover transitions, a clearer token-driven keyboard focus ring, and reduced-motion-aware `scale(0.96)` press feedback.
  - Kept short editorial labels and their directional badges together by giving the label an explicit wrapper, preventing unnecessary wrapping when the surrounding card has room.
  - Allowed long translated labels to wrap within narrow containers without pushing the CTA beyond the viewport, while positioning the decorative directional badge independently so it cannot distort label wrapping.
  - Kept the public anchor markup flat by applying CTA wrapping directly to the link instead of introducing a label-only child element.
  - Corrected the all-variants story's Hero fixture so its gradient overlay stays behind the dark-context controls instead of covering the Storybook canvas, and added the missing editorial CTA treatment to both light and Hero comparisons.
- **1.3** — 2026-08-26: Add composable `.mg-button-outline` modifier for primary and secondary emphasis variants.
- **1.2** — 2026-04-13: `.mg-button` now declares an explicit `font-family: $mg-font-family-headings;` (was inheriting body Roboto). Aligns with the existing `:lang(ar)` override which already used `$mg-font-family-arabic-headings` — Latin and Arabic now match.
- **1.1** — 2026-03-22: Add "All variants" comparison story (light + dark backgrounds). Fix secondary button contrast to meet WCAG AA (blue-800/blue-700, was blue-600/blue-400). Fix disabled button contrast (neutral-500 bg + white text). Remove dead `mg-button-arrow` class and `For_Primary` prop. Add `:focus-visible` ring for buttons on dark backgrounds. Tokenize secondary button and hero secondary button colors for theme overriding.
- **1.0** — 2022-12-12: Released component
