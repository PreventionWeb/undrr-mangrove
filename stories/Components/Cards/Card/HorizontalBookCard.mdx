import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as HorizontalBookCardStories from './HorizontalBookCard.stories';

<Meta of={HorizontalBookCardStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Horizontal book card

A horizontal card variant for publications. Displays a portrait cover image on the left with title, summary, labels, and a CTA on the right. Ideal for detailed publication listings where you need both a cover image and descriptive text.

## When to use

- Publication detail listings with summaries
- Resource pages where cover art is important alongside descriptive text
- Search results for documents, reports, and brochures

## Anatomy

- **Cover image** (optional): Portrait-oriented, constrained to 200px on mobile, natural width on desktop
- **Labels** (optional): Content type or category tags
- **Title**: Linked heading with blue color, underline, and chevron indicator
- **Summary**: Body text, supports inline HTML links
- **CTA button** (optional): Primary action button

The entire card is clickable via a stretched link on the title.

<Canvas of={HorizontalBookCardStories.DefaultHorizontalBookCard} />

### Without image

When no cover image is available, the content area fills the full card width.

<Canvas of={HorizontalBookCardStories.HorizontalBookCardNoImageVerticalCard} />

## Color variants

Use the `variant` prop to change the title accent color.

| Variant             | Class                 | Color                          |
| ------------------- | --------------------- | ------------------------------ |
| `primary` (default) | —                     | `$mg-color-interactive` (blue) |
| `secondary`         | `mg-card--secondary`  | `$mg-color-secondary` (orange) |
| `tertiary`          | `mg-card--tertiary`   | `$mg-color-tertiary` (black)   |
| `quaternary`        | `mg-card--quaternary` | `$mg-color-quaternary` (red)   |

## Title link visibility

Card titles display blue color, underline, and a chevron indicator by default. Add `mg-card--plain-title` to disable these signals.

**In React:**

```jsx
<HorizontalBookCard data={data} className="mg-card--plain-title" />
```

**In Drupal templates or static HTML:**

```html
<article class="mg-card mg-card__hc mg-card-book__hc mg-card--plain-title">
  ...
</article>
```

## Responsive behavior

| Breakpoint       | Layout                                                         |
| ---------------- | -------------------------------------------------------------- |
| Mobile (< 640px) | Stacked: cover image (max 200px wide) above content            |
| Tablet (640px+)  | Side-by-side: 160px cover image, content fills remaining width |

## Cards without links

A card should almost always be a link, but unlinked ones happen: an editor leaves the link field empty, a referenced node is unpublished, or the card exists only to carry a button.

Omit `link` (or pass an empty string) and the card renders its title as plain text — no chevron, no underline, no interactive colour, no stretched click target, no hover elevation. Labels stay visible but drop the interactive modifier. The article gains `mg-card--no-link` so themes have a hook.

<Canvas of={HorizontalBookCardStories.NoLinkHorizontalBookCard} />

### A button instead of a card link

For a card whose only destination is its button, set `buttonLink` and leave `link` empty. The title stays plain text and the CTA is an ordinary in-flow link. `buttonLink` falls back to `link` when omitted, so existing cards are unaffected.

<Canvas of={HorizontalBookCardStories.ButtonOnlyHorizontalBookCard} />

Drupal Gutenberg emits `<a href="">` for unlinked cards, which a browser treats as a link to the current page: it takes focus, activating it reloads the page, and the stretched overlay swallows clicks aimed at the card's own button. `card.scss` neutralises that markup so published content is repaired without an upstream change, but CSS cannot take the anchor out of the tab order — emit a plain-text title at the source.

See [Vertical card](?path=/docs/components-cards-vertical-card--docs#cards-without-links) for the full rationale and the `href=""` reference case.

## Props

| Prop        | Type   | Default     | Description                                                             |
| ----------- | ------ | ----------- | ----------------------------------------------------------------------- |
| `data`      | Array  | Required    | Array of card data objects                                              |
| `variant`   | String | `'primary'` | Color variant: `'primary'`, `'secondary'`, `'tertiary'`, `'quaternary'` |
| `className` | String | —           | Additional CSS classes on the card element                              |

### Data object properties

| Property      | Type   | Description                                                                                             |
| ------------- | ------ | ------------------------------------------------------------------------------------------------------- |
| `title`       | String | Card heading text                                                                                       |
| `link`        | String | URL for the title link and stretched link. Omit it, or pass an empty string, to render an unlinked card |
| `imgback`     | String | Cover image URL                                                                                         |
| `imgalt`      | String | Alt text for the cover image                                                                            |
| `summaryText` | String | Body text (HTML supported)                                                                              |
| `label1`      | String | First category label                                                                                    |
| `label2`      | String | Second category label                                                                                   |
| `button`      | String | CTA button label                                                                                        |
| `buttonLink`  | String | CTA destination. Defaults to `link`; set it to give an unlinked card a button                           |

## Changelog

- **1.4.0** — 2026-09-10: Unlinked cards now render plain-text titles (not empty anchors), keep labels visible, and support `buttonLink` for CTA-only destinations. `card.scss` also removes link affordances from empty/missing `href` markup. See [Vertical card](?path=/docs/components-cards-vertical-card--docs#cards-without-links).
- **1.3.4** — 2026-09-09 ([#1098](https://github.com/unisdr/undrr-mangrove/issues/1098)): Arabic title/label typography now follows the `ui` font role (`Noto Sans Arabic`), and the component-level `:lang(ar)` override was removed. Latin rendering is unchanged.
- **1.3.3** — 2026-09-08 ([#1092](https://github.com/unisdr/undrr-mangrove/issues/1092)): Arabic typography no longer applies to a card title or label whose own language is not Arabic.
- **1.3.2** — 2026-09-01 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)):
  - Unified focus states behind the theme-adaptive `mg-focus-ring` mixin and added a matching 2px radius to title-link focus rings.
  - Added the shared raised card surface, elevation motion, and improved title/body wrapping through semantic modernisation tokens.
- **1.3.1** — 2026-07-08: Fixed z-index conflict with global navigation (MegaMenu) by creating local stacking context via `isolation: isolate` and lowering card-local z-indices from 100/101 to 1/2.
- **1.3** — 2026-04-13: `.mg-card__title` and `.mg-card__label` now use `$mg-font-family-headings` (Roboto Condensed). Added a `:lang(ar)` block routing both to `$mg-font-family-arabic-headings`. Same change applies to all card variants.
- **1.2.1** — 2026-03-12: Fix chevron caret orphaning on its own line. Trim trailing whitespace from title text.
- **1.2** — 2026-03-05: Title links now show blue color, underline, and chevron indicator for better affordance. Added `variant` prop (primary/secondary/tertiary/quaternary) matching the Hero color system. Removed dead `Hovercolors` prop. Added `mg-card--plain-title` CSS opt-out modifier.
- **1.1.1** — 2025-12-12: Removed unused card share support (`item.share` / `mg-card__share`)
- **1.1** — 2024-09-20: Handle case when image is not available
- **1.0** — 2023-09-04: Released component
