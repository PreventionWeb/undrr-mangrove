import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as HorizontalBookCardStories from './HorizontalBookCard.stories';

<Meta of={HorizontalBookCardStories} />

> If you are creating or modifying this component, see the [review checklist](https://mangrove.undrr.org/?path=/docs/contributing-build-a-component-review-checklist--docs) for Mangrove's component standards.

# Horizontal book card

A horizontal card variant for publications. Displays a portrait cover image on the left with title, summary, labels, and a CTA on the right. Ideal for detailed publication listings where you need both a cover image and descriptive text.

## When to use

- Publication detail listings with summaries
- Resource pages where cover art is important
- Search results for documents, reports, and brochures

## Anatomy

- **Cover image** (optional): Portrait-oriented, 200px on mobile, natural width on desktop
- **Labels** (optional): Content type or category tags
- **Title**: Linked heading with blue color, underline, and chevron
- **Summary**: Body text with HTML link support
- **CTA button** (optional): Primary action

The entire card is clickable via a stretched link on the title.

<Canvas of={HorizontalBookCardStories.DefaultHorizontalBookCard} />

### Without image

When no cover image is available, the content area fills the full card width.

<Canvas of={HorizontalBookCardStories.HorizontalBookCardNoImageVerticalCard} />

## Color variants

Use the `variant` prop to change the title accent color.

| Variant             | Class                 | Color                                                                                    |
| ------------------- | --------------------- | ---------------------------------------------------------------------------------------- |
| `primary` (default) | —                     | `$mg-color-interactive` (blue)                                                           |
| `secondary`         | `mg-card--secondary`  | Title `$mg-color-interactive` (blue), plus a `$mg-color-secondary` (orange) leading rule |
| `tertiary`          | `mg-card--tertiary`   | `$mg-color-tertiary` (black)                                                             |
| `quaternary`        | `mg-card--quaternary` | `$mg-color-quaternary` (red)                                                             |

**Contrast:** the orange accent carries no text, in either direction. `secondary` used to paint the title with it, at 2.65 against WCAG's 4.5 on the card surface; the title now takes the interactive colour and the accent is a 4px rule on the card's leading edge. That rule is 2.95 against white, under the 3:1 of SC 1.4.11, so it is emphasis rather than the only signal. See [the colour contrast methodology](https://github.com/unisdr/undrr-mangrove/blob/main/docs/COLOUR-CONTRAST-METHODOLOGY.md#the-orange-accent-carries-no-text) for the measurements.

## Title link visibility

Card titles show: blue color, underline, and a chevron indicator. Add `mg-card--plain-title` to disable these signals.

**React:** `<HorizontalBookCard data={data} className="mg-card--plain-title" />`

**HTML:** `<article class="mg-card mg-card__hc mg-card-book__hc mg-card--plain-title">...</article>`

## Responsive behavior

| Breakpoint       | Layout                                                         |
| ---------------- | -------------------------------------------------------------- |
| Mobile (< 640px) | Stacked: cover image (max 200px wide) above content            |
| Tablet (640px+)  | Side-by-side: 160px cover image, content fills remaining width |

## Cards without links

Omit `link` (or pass empty string) and the card renders a plain-text title: no chevron, underline, color, stretched link, or hover effect. Labels stay visible.

<Canvas of={HorizontalBookCardStories.NoLinkHorizontalBookCard} />

### A button instead of a card link

Set `buttonLink` and leave `link` empty. The title stays plain text and the CTA is an in-flow link.

<Canvas of={HorizontalBookCardStories.ButtonOnlyHorizontalBookCard} />

See [Vertical card](?path=/docs/components-cards-vertical-card--docs#cards-without-links) for the full rationale and `href=""` reference.

## Props

| Prop        | Type   | Default     | Description                                                             |
| ----------- | ------ | ----------- | ----------------------------------------------------------------------- |
| `data`      | Array  | Required    | Array of card data objects                                              |
| `variant`   | String | `'primary'` | Color variant: `'primary'`, `'secondary'`, `'tertiary'`, `'quaternary'` |
| `className` | String | —           | Additional CSS classes on the card element                              |

### Data object properties

| Property      | Type   | Description                                                                                             |
| ------------- | ------ | ------------------------------------------------------------------------------------------------------- |
| `title`       | String | Card heading text                                                                                       |
| `link`        | String | URL for the title link and stretched link. Omit it, or pass an empty string, to render an unlinked card |
| `imgback`     | String | Cover image URL                                                                                         |
| `imgalt`      | String | Alt text for the cover image                                                                            |
| `summaryText` | String | Body text (HTML supported)                                                                              |
| `label1`      | String | First category label                                                                                    |
| `label2`      | String | Second category label                                                                                   |
| `button`      | String | CTA button label                                                                                        |
| `buttonLink`  | String | CTA destination. Defaults to `link`; set it to give an unlinked card a button                           |

## Changelog

- **1.5.0**, 2026-09-18 ([#1249](https://github.com/unisdr/undrr-mangrove/pull/1249)): Fixes [#1196](https://github.com/unisdr/undrr-mangrove/issues/1196). `mg-card--secondary` no longer paints its title in the orange accent (2.65 on the card surface, 2.37 on DELTA's). The title takes the interactive colour, and the accent becomes a 4px rule on the card's leading edge instead. `--mg-color-secondary` is unchanged. This is a visible change wherever the modifier is used, in all five themes.
- **1.4.1**, 2026-09-17 ([#1190](https://github.com/unisdr/undrr-mangrove/pull/1190)): Links inside a card summary (`.mg-card__text-link`, and any anchor in `.mg-card__summary`) are underlined at rest instead of only on `:hover`. At 2.09:1 against the body colour, the interactive blue could not distinguish them on its own (WCAG 1.4.1 Use of Colour), and `link-in-text-block` was the most frequent axe violation in the 2.0 pre-release sweep. The underline thickens on hover. Button-styled links are excluded. Card titles are untouched: a linked title has always been underlined, and the `--plain-title` and empty-`href` opt-outs that remove that underline still do.
- **1.4.0**, 2026-09-10: Plain-text titles for unlinked cards, visible labels, `buttonLink` prop, and `card.scss` fixes for empty `href`.
- **1.3.4**, 2026-09-09 ([#1098](https://github.com/unisdr/undrr-mangrove/issues/1098)): Arabic titles/labels use `ui` font role.
- **1.3.3**, 2026-09-08 ([#1092](https://github.com/unisdr/undrr-mangrove/issues/1092)): Language-specific Arabic typography.
- **1.3.2**, 2026-09-01 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)): Unified focus states, raised surface, elevation.
- **1.3.1**, 2026-07-08: Fix z-index conflict with MegaMenu.
- **1.3**, 2026-04-13: Roboto Condensed headings; add `variant` prop.
- **1.2.1**, 2026-03-12: Fix chevron orphaning; trim title whitespace.
- **1.2**, 2026-03-05: Link affordances; `variant` prop; `mg-card--plain-title` modifier.
- **1.1.1**, 2025-12-12: Remove card share support.
- **1.1**, 2024-09-20: Handle missing images.
- **1.0**, 2023-09-04: Initial release.
