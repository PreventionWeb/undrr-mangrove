import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as BookCardStories from './BookCard.stories';

<Meta of={BookCardStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Book card

A compact card variant for publications and brochures. Displays a portrait cover image (3:4 aspect ratio) with a title below. Designed to fit 5-6 across in a row on desktop.

## When to use

- Publication and report listings
- Library/resource pages with cover images
- Grids with portrait-oriented visuals

## Anatomy

- **Cover image**: 3:4 aspect ratio with subtle border
- **Title**: Linked heading with smaller font than other card variants

The entire card is clickable via a stretched link on the title.

<Canvas of={BookCardStories.DefaultBookCard} />

## Books together

Different title lengths share equal heights within each row. Covers keep proportions, titles start below cover. Resize to test wrapping.

<Canvas of={BookCardStories.MixedTitleLengths} />

## Color variants

Use the `variant` prop to change the title accent color.

| Variant             | Class                 | Color                          |
| ------------------- | --------------------- | ------------------------------ |
| `primary` (default) | —                     | `$mg-color-interactive` (blue) |
| `secondary`         | `mg-card--secondary`  | `$mg-color-secondary` (orange) |
| `tertiary`          | `mg-card--tertiary`   | `$mg-color-tertiary` (black)   |
| `quaternary`        | `mg-card--quaternary` | `$mg-color-quaternary` (red)   |

## Title link visibility

Card titles show: blue color, underline, and a chevron indicator. Add `mg-card--plain-title` to disable these signals.

**React:** `<BookCard data={data} className="mg-card--plain-title" />`

**HTML:** `<article class="mg-card mg-card__vc mg-card__book mg-card--plain-title">...</article>`

## Sizing

Card is constrained to 200px max-width by default, expanding to 400px on desktop. In a grid, grid column width controls card size.

## Cards without links

Omit `link` (or pass empty string) and the card renders a plain-text title: no chevron, underline, color, stretched link, or hover effect.

<Canvas of={BookCardStories.NoLinkBookCard} />

See [Vertical card](?path=/docs/components-cards-vertical-card--docs#cards-without-links) for the full rationale and `href=""` reference.

## Props

| Prop        | Type   | Default     | Description                                                             |
| ----------- | ------ | ----------- | ----------------------------------------------------------------------- |
| `data`      | Array  | Required    | Array of card data objects                                              |
| `variant`   | String | `'primary'` | Color variant: `'primary'`, `'secondary'`, `'tertiary'`, `'quaternary'` |
| `className` | String | —           | Additional CSS classes on the card element                              |

### Data object properties

| Property  | Type   | Description                                                                                             |
| --------- | ------ | ------------------------------------------------------------------------------------------------------- |
| `title`   | String | Card heading text                                                                                       |
| `link`    | String | URL for the title link and stretched link. Omit it, or pass an empty string, to render an unlinked card |
| `imgback` | String | Cover image URL                                                                                         |
| `imgalt`  | String | Alt text for the cover image                                                                            |

## Changelog

- **1.4.0** — 2026-09-10: Plain-text titles for unlinked cards, and `card.scss` fixes for empty `href`.
- **1.3.5** — 2026-09-09 ([#1109](https://github.com/unisdr/undrr-mangrove/issues/1109)): Responsive collection with varied title lengths.
- **1.3.4** — 2026-09-09 ([#1098](https://github.com/unisdr/undrr-mangrove/issues/1098)): Arabic titles use `ui` font role.
- **1.3.3** — 2026-09-08 ([#1092](https://github.com/unisdr/undrr-mangrove/issues/1092)): Language-specific Arabic typography.
- **1.3.2** — 2026-09-01 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)): Unified focus states, raised surface, elevation.
- **1.3.1** — 2026-07-08: Fix z-index conflict with MegaMenu.
- **1.3** — 2026-04-13: Roboto Condensed headings; add `variant` prop.
- **1.2** — 2026-03-14 ([web-backlog#2630](https://gitlab.com/undrr/web-backlog/-/work_items/2630)): Themeable card tokens.
- **1.1.1** — 2026-03-12: Fix chevron orphaning; trim title whitespace.
- **1.1** — 2026-03-05: Link affordances; `variant` prop; `mg-card--plain-title` modifier.
- **1.0.2** — 2025-12-12: Remove card share support.
- **1.0.1** — 2025-01-09: Responsive fixes.
- **1.0** — 2023-03-01: Initial release.
