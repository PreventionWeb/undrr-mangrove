import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as TextareaStories from './Textarea.stories';

<Meta of={TextareaStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Textarea

A multi-line text field for longer user input.

## Overview

The textarea component is a resizable multi-line input with label, help text, and error state. It follows the same accessibility patterns as the text input component.

## When to use

- When the user needs to enter multi-line text such as comments, descriptions, or feedback.

## States

- **Default**: Ready for input.
- **Focus**: Blue outline on keyboard focus.
- **Disabled**: Greyed out, non-interactive.
- **Error**: Red border with an error message below.

<Canvas of={TextareaStories.Default} />

<Controls of={TextareaStories.Default} />

### With help text

<Canvas of={TextareaStories.WithHelpText} />

### Error state

<Canvas of={TextareaStories.ErrorState} />

### Disabled

<Canvas of={TextareaStories.Disabled} />

### Required

<Canvas of={TextareaStories.Required} />

## Accessibility

- The label is programmatically associated with the textarea via `htmlFor`/`id`.
- Help text and error text are linked via `aria-describedby`.
- Error state sets `aria-invalid="true"` and wraps the error message in `role="alert"`.
- The textarea is resizable vertically by default.

## See also

- [Form validation example](?path=/docs/example-form-validation--docs) — working form with error summary, inline errors, and toast mode

## Changelog

- **2.1.1** — 2026-09-08 ([#1092](https://github.com/unisdr/undrr-mangrove/issues/1092)): Arabic typography no longer applies to a textarea whose own language is not Arabic.
- **2.1** — 2026-09-01 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)): Adopted the shared light filled field surface, subtle neutral border, small radius and token-driven focus ring.
- **2.0** — 2026-02-24: **Breaking:** Rewritten with BEM classes, accessibility, and PropTypes
- **1.0** — 2022-12-12: Initial release
