import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { Hero } from './Hero';
import * as HeroStories from './Hero.stories';

<Meta of={HeroStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Mangrove hero (main)

Hero is a full-width page section with title, summary, and optional CTA buttons.
It supports two layouts and four color variants.

## Overview

### Layouts

**`layout="background"` (default):** Full-bleed background image plus overlay; use for campaign/homepage moments.

**`layout="split"`:** Theme-colored background with text and media columns; use for topic pages and section intros. Content stays first in DOM order (LTR/RTL).

### Heading level

`headingLevel` sets the title element (`h1`, `h2`, `h3`; default `h1`). Use
`h1` for page-primary heading and `h2`/`h3` for mid-page heroes.

### Media slot (split layout only)

The media column accepts three shapes, discriminated by `media.type`:

| `media.type`        | Required fields                            | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `"image"` (default) | `src`, `alt` (empty string for decorative) | Rendered as a cover-fit `<img>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `"video"`           | `src` (iframe-embeddable URL), `title`     | Rendered as a 16:9 `<iframe>`. Use the provider's embed URL (e.g. `https://www.youtube.com/embed/…`). `title` is required for screen reader users.                                                                                                                                                                                                                                                                                                                                                |
| `"html"`            | `html` (pre-sanitized HTML string)         | Rendered via `dangerouslySetInnerHTML`. The **consumer must sanitize** — Mangrove does not escape this string. Use for custom embeds, figures with attribution overlays, or any markup that doesn't fit image/video. A `<figure>` dropped directly into the slot is styled by Mangrove as a full-bleed image with an attribution strip overlay. Wrap content in your own container (as in the example below) to add padding, decoration, or composition on top of the hero's coloured background. |

If `media.type` is omitted, Hero falls back to `image` for backward compatibility.

### Column split (split layout only)

The `split` prop controls the content/media column ratio, aligned to the MG 12-column grid:

| `split`           | Content | Media  | Use case                   |
| ----------------- | ------- | ------ | -------------------------- |
| `"2/3"` (default) | 8 cols  | 4 cols | Text-heavy topic pages     |
| `"1/2"`           | 6 cols  | 6 cols | Balanced content and image |
| `"1/3"`           | 4 cols  | 8 cols | Media-led, image dominates |

### When to use

- Use `layout="background"` for visually-led pages where the image is central to the brand moment.
- Use `layout="split"` when you need dense body copy alongside a representative image (e.g., topic pages, campaign sections).

## Example — background (default)

<Canvas of={HeroStories.Default} meta={HeroStories} />

## Example — split layout

<Canvas of={HeroStories.SplitDefault} meta={HeroStories} />

## Example — split with video

<Canvas of={HeroStories.SplitWithVideo} meta={HeroStories} />

## Example — split with custom HTML

<Canvas of={HeroStories.SplitWithHtml} meta={HeroStories} />

## Example — immersive and contained

Use `size="immersive"` for tall editorial layouts and `contained` when Hero
should respect parent width. Content can include `logo` and `buttons`.

<Canvas of={HeroStories.ImmersiveContained} meta={HeroStories} />

## Interactions

- Title links (when `link` is set) and CTA buttons navigate to their destinations.

## Changelog

- **1.6.4** — 2026-09-10 ([undrr/web-backlog#3060](https://gitlab.com/undrr/web-backlog/-/work_items/3060)):
  - Removed restrictive title width caps and aligned split-layout spacing to shared container rhythm.
  - Simplified title markup/styles by removing dead wrappers and applying `mg-hero__title` directly to the heading.
- **1.6.3** — 2026-09-10 ([#1113](https://github.com/unisdr/undrr-mangrove/pull/1113)):
  - Background gradient now follows `--mg-color-hero` instead of a hardcoded blue, so themed brands render correctly. ([#1115](https://github.com/unisdr/undrr-mangrove/issues/1115))
  - Scoped link color rules to hero text regions (`__overlay`/`__content`) so consumer HTML media content is not unintentionally restyled.
- **1.6.2** — 2026-09-09 ([#1098](https://github.com/unisdr/undrr-mangrove/issues/1098)): Arabic typography now follows font roles (`display` for title, `ui` for label) with non-Arabic rendering unchanged.
- **1.6.1** — 2026-09-08 ([#1092](https://github.com/unisdr/undrr-mangrove/issues/1092)): Arabic typography no longer applies to a hero title or label whose own language is not Arabic.
- **1.6** — 2026-09-01 ([#1086](https://github.com/unisdr/undrr-mangrove/pull/1086)):
  - Refreshed directional gradient for better contrast and theme control in LTR/RTL.
  - Improved CTA spacing/responsiveness while preserving variant behavior.
  - Added contained/immersive options, logo support, and structured CTA data.
- **1.5** — 2026-04-15 ([#942](https://github.com/unisdr/undrr-mangrove/issues/942)): Added `layout="split"` with `headingLevel` and `split` controls, plus image/video/html media slot support.
- **1.4** — 2026-04-13: `.mg-hero__title` and `.mg-hero__label` now use `$mg-font-family-headings` (Roboto Condensed). Added a `:lang(ar)` block routing both to `$mg-font-family-arabic-headings`.
- **1.3** — 2026-03-14 ([web-backlog#2630](https://gitlab.com/undrr/web-backlog/-/work_items/2630)): Hero title color is now a theme token (`$mg-color-hero-title`, default white). Overlay width increased from `$mg-width-400` (40rem) to `$mg-width-600` (60rem) and padding from `$mg-spacing-200` to `$mg-spacing-400` for better content readability across all themes.
- **1.2** — 2024-09-20: Conditional rendering of headline (anchor tag when link is present, header tag otherwise); added HTML support in title and summaryText fields
- **1.1** — 2024-09-20: Locale-based content switching with `getCaptionForLocale`; conditional headline rendering; HTML support in text fields
- **1.0** — 2023-01-24: Initial release with customizable background images, text, and CTA buttons
