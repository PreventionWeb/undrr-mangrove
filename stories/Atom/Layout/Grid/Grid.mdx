import { Canvas, Meta, Story, Source } from '@storybook/addon-docs/blocks';
import * as GridStories from './Grid.stories';

<Meta
  of={GridStories}
  title="Design decisions/Grid layout"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

> If you are creating or modifying this component, see the [review checklist](https://mangrove.undrr.org/?path=/docs/contributing-build-a-component-review-checklist--docs) for Mangrove's component standards.

# Grid system

This grid system is based on CSS Grid. The grid comes in two main layouts: auto layout and specified columns layout such as col-2.

<Canvas of={GridStories.DefaultGrid} meta={GridStories} />

### Usage

Use the grid to create a layout of your choice based on the design you want to achieve.

- copy the HTML of the variant you want and add the CSS file below to get started.

#### HTML for AUTO LAYOUT VARIANT:

```html
<div class="mg-grid"></div>
```

#### HTML for SPECIFIED COLUMN(S) LAYOUT VARIANT:

```html
<div class="mg-grid mg-grid__col-3"></div>
```

#### HTML for the asymmetric ARTICLE VARIANT:

Every variant above splits its space into equal fractions. `mg-grid--article`
is a different shape — a flexible column plus a fixed-width rail — for a page
layout like a reading column with a sidebar, not a card grid. It is what
powers `mg-reading--with-contents` (see the [Reading column](?path=/docs/components-reading-column--docs)
doc); reach for it directly if you need the same shape outside that pattern.

```html
<div class="mg-grid mg-grid--article">
  <div><!-- flexible column --></div>
  <div><!-- 16rem fixed-width rail --></div>
</div>
```

#### CSS:

There is no standalone grid stylesheet. The grid rules are compiled into every
theme stylesheet, so a page that already loads one has them:

```html
<link
  rel="stylesheet"
  href="https://assets.undrr.org/mangrove/2.0.0-rc.2/css/style.css"
/>
```

Sass consumers who want the grid on its own can import the partial the theme
stylesheets import, `@undrr/undrr-mangrove/scss/Atom/Layout/Grid/grid`. It
carries the same prerequisites as any other per-component partial — see
[Sass integration](?path=/docs/getting-started-integration-sass-integration--docs).

## Changelog

- **1.3** — 2026-09-19 ([#1266](https://github.com/unisdr/undrr-mangrove/issues/1266)): The CSS section pointed at `.../assets/css/grid.min.css`, which the CDN has never served — no standalone grid stylesheet is emitted, the `assets/` segment is doubled, and nothing under Mangrove is minified. It now names the theme stylesheet the grid rules compile into, and the SCSS partial for Sass consumers. Documentation only; the component is unchanged.
- **1.2** — 2026-09-11 ([undrr/web-backlog#3060](https://gitlab.com/undrr/web-backlog/-/work_items/3060)): Added `.mg-grid--article` for reading-layout column splits (flexible content column + fixed rail), extracted for reuse from page-pattern-specific CSS.
- **1.1** — 2026-08-27: Added `.mg-grid__col-7` through `.mg-grid__col-12` and `.mg-grid--auto-fit` to support larger or dynamic item counts.
- **1.0** — 2022-12-12: Released component
