import { Canvas, Meta, Story, Source } from '@storybook/addon-docs/blocks';
import * as GridStories from './Grid.stories';

<Meta
  of={GridStories}
  title="Design decisions/Grid layout"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Grid system

This grid system is based on CSS Grid. The grid comes in two main layouts: auto layout and specified columns layout such as col-2.

<Canvas of={GridStories.DefaultGrid} meta={GridStories} />

### Usage

Use the grid to create a layout of your choice based on the design you want to achieve.

- copy the HTML of the variant you want and add the CSS file below to get started.

#### HTML for AUTO LAYOUT VARIANT:

```html
<div class="mg-grid"></div>
```

#### HTML for SPECIFIED COLUMN(S) LAYOUT VARIANT:

```html
<div class="mg-grid mg-grid__col-3"></div>
```

#### CSS:

Add the base layout style from

- https://assets.undrr.org/static/mangrove/1.8.1/assets/css/grid.min.css

## Changelog

- **1.1** — 2026-08-27: Extended the numbered column classes to `.mg-grid__col-7` through `.mg-grid__col-12`, and added the `.mg-grid--auto-fit` modifier that distributes any item count across a single row. Prevents grids whose column class is derived from a dynamic item count (e.g. StatsCard) from collapsing to a stacked single column past 6.
- **1.0** — 2022-12-12: Released component
